{% extends 'base.html.twig' %}

{% block body %}
{% include('navbar.html.twig') %}
<div class="container">
    <h1>Clientes</h1>
    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ empresa.id }}</td>
            </tr>
            <tr>
                <th>RUC</th>
                <td>{{ empresa.ruc }}</td>
            </tr>
            <tr>
                <th>RAZON SOCIAL</th>
                <td>{{ empresa.razonSocial }}</td>
            </tr>
            <tr>
                <th>DIRECCION</th>
                <td>{{ empresa.direccion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('empresa_index') }}" class="btn btn-info btn-sm">VOLVER</a>

    <a href="{{ path('empresa_edit', {'id': empresa.id}) }}" class="btn btn-warning btn-sm">EDITAR</a>

    {{ include('empresa/_delete_form.html.twig') }}
</div>
{% endblock %}
